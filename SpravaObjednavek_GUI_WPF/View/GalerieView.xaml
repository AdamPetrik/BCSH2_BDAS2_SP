<UserControl x:Class="SpravaObjednavek_GUI_WPF.View.GalerieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpravaObjednavek_GUI_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F0F0F0">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Galerie položek" FontSize="24" FontWeight="Light" Margin="0,0,0,20"/>

                <ItemsControl ItemsSource="{Binding Polozky}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Width="220" Margin="10" Padding="15" CornerRadius="5" BorderBrush="#DDD" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding NazevJidla}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" TextWrapping="Wrap" TextAlignment="Center"/>

                                    <Image Source="{Binding ObrazekSource}" Height="150" Stretch="Uniform" 
                                           Visibility="{Binding MaObrazek, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <TextBlock Text="Bez obrázku" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#CCC" FontStyle="Italic">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding MaObrazek}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <Button Content="STÁHNOUT" 
                                            Command="{Binding DataContext.StahnoutCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            IsEnabled="{Binding MaObrazek}"
                                            Margin="0,10,0,0" Padding="10,5" Background="#2196F3" Foreground="White" FontWeight="Bold" BorderThickness="0">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
