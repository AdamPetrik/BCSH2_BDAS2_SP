<UserControl x:Class="SpravaObjednavek_GUI_WPF.View.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpravaObjednavek_GUI_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F0F0F0">
        <Border Background="White" Margin="20" CornerRadius="5" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Systémové Logy (Audit)" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding SeznamLogu}" 
                          AutoGenerateColumns="False" IsReadOnly="True" 
                          BorderBrush="#DDD" BorderThickness="1"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>

                        <DataGridTextColumn Header="Čas" Binding="{Binding Cas, StringFormat=\{0:dd.MM.yyyy HH:mm:ss\}}" Width="140"/>

                        <DataGridTextColumn Header="Uživatel" Binding="{Binding Uzivatel}" Width="120" FontWeight="SemiBold"/>

                        <DataGridTextColumn Header="Akce" Binding="{Binding Akce}" Width="80" FontWeight="Bold"/>

                        <DataGridTextColumn Header="Tabulka" Binding="{Binding Tabulka}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
