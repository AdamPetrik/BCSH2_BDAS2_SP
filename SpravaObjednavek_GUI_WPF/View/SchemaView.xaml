<UserControl x:Class="SpravaObjednavek_GUI_WPF.View.SchemaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpravaObjednavek_GUI_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F0F0F0">
        <Border Background="White" Margin="20" CornerRadius="5" Padding="20">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Seznam databázových objektů (Systémový katalog)" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding SeznamObjektu}" AutoGenerateColumns="False" IsReadOnly="True" BorderBrush="#DDD" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Typ" Binding="{Binding Typ}" Width="150" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Název objektu" Binding="{Binding Jmeno}" Width="*"/>
                        <DataGridTextColumn Header="Vytvořeno" Binding="{Binding DatumVytvoreni, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="2" Text="Data jsou čtena přímo ze systémových tabulek (USER_OBJECTS)." 
                           Foreground="Gray" Margin="0,10,0,0" FontStyle="Italic"/>
            </Grid>

        </Border>
    </Grid>
</UserControl>
