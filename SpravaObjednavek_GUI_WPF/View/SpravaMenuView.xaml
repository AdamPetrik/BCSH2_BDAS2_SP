<UserControl x:Class="SpravaObjednavek_GUI_WPF.View.SpravaMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpravaObjednavek_GUI_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White" Padding="20" Margin="20" HorizontalAlignment="Left" VerticalAlignment="Top" Width="500">
        <StackPanel>
            <TextBlock Text="Nová položka menu" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Název jídla" Foreground="Gray"/>
            <TextBox Text="{Binding NazevPolozky}" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Cena (Kč)" Foreground="Gray"/>
            <TextBox Text="{Binding CenaPolozkyInput}" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Obrázek" Foreground="Gray"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="VYBRAT SOUBOR..." Command="{Binding VybratObrazekCommand}" Padding="10,5"/>
                <TextBlock Text="{Binding CestaKObrazku}" VerticalAlignment="Center" Margin="10,0,0,0" FontStyle="Italic"/>
            </StackPanel>

            <Border BorderBrush="#DDD" BorderThickness="1" Height="150" Width="200" HorizontalAlignment="Left" Margin="0,0,0,20">
                <Image Source="{Binding NahledObrazku}" Stretch="Uniform"/>
            </Border>

            <Button Content="ULOŽIT POLOŽKU" Command="{Binding UlozitPolozkuCommand}" 
                    Background="#4CAF50" Foreground="White" FontWeight="Bold" Height="40"/>
        </StackPanel>
    </Border>
</UserControl>
